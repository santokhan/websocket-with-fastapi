[project]
name = "websocket-with-fastapi"
version = "0.1.0"
description = "FastAPI text and image WebSocket example"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
]
